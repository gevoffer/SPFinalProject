1. לכתוב פקודות אלוקציה ושחרור 
2. הדפסת לוח
3. מציאת בלוק, שורה, עמודה
4. כל הפקודות שהן לא חכמות (solve, validate, ganarate) ולא עבודה עם קבצים, כולל parsing של המשתמש
5. פקודות solve, validate, generate- אחת אחרי השניה + בדיקות אחרי כל אחת
6. עבודה עם קבצים


חלוקת קבצים:
קובץ של כל מה שמשותף לכל המודים- backend (מבנים, אלוקציה ושחרור זכרון, ...)
קובץ משחק כללי (מה שמשותף לכל המודים)
קובץ לכל מוד
קובץ פרסר- קלט
קובץ עבודה עם קבצים
קובץ של solver, validator, generator
main

