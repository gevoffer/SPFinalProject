Inbar:
- Set (update blocks, update moveslist)
- Other commands

Gev:
- Implement parser
- Work with files

Next step:
figure out which functions are a must before the "smart commands" (solve, validate, generate etc)
complete them
and afterwards complete the smart commands


חלוקת קבצים:
קובץ של כל מה שמשותף לכל המודים- backend (מבנים, אלוקציה ושחרור זכרון, ...)
קובץ משחק כללי (מה שמשותף לכל המודים)
קובץ לכל מוד
קובץ פרסר- קלט
קובץ עבודה עם קבצים
קובץ של solver, validator, generator
main