1. find block, row and column
2. parcing user input
3. all commands that are not smart commands (solve, validate, generate)
4. smart commands
5. working with files

TODO:
1. list allocation and deletion -> undo commands in the end


חלוקת קבצים:
קובץ של כל מה שמשותף לכל המודים- backend (מבנים, אלוקציה ושחרור זכרון, ...)
קובץ משחק כללי (מה שמשותף לכל המודים)
קובץ לכל מוד
קובץ פרסר- קלט
קובץ עבודה עם קבצים
קובץ של solver, validator, generator
main

